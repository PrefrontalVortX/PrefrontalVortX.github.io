# Prefrontal VortX — corrected live version

Upload `index.html` and `styles.css` to the root of your GitHub repository, replacing the prior versions.

In GitHub:
- Settings → Pages
- Source: Deploy from a branch
- Branch: main
- Folder: / (root)

Keep your custom domain set to `prefrontalvortx.com`.
